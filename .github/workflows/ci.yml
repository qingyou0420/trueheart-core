name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install development tools
        run: python -m pip install --upgrade pip && python -m pip install -e ".[dev]"
      - name: Run tests
        run: python -m pytest tests -q
      - name: Run Ruff check
        run: python -m ruff check src/trueheart_core tests examples
      - name: Run Ruff format check
        run: python -m ruff format --check src/trueheart_core tests examples
      - name: Run mypy
        run: python -m mypy src/trueheart_core
      - name: Build distributions
        run: python -m build
      - name: Reinstall wheel
        run: python -m pip install --force-reinstall dist/trueheart_core-0.1.2-py3-none-any.whl
      - name: Run installed example
        run: python examples/basic_memory.py
